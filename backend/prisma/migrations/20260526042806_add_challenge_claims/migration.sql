BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[challenges] ADD [challengedClaim] NVARCHAR(1000),
[challengerClaim] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
